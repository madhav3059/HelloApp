public class GreetingApp {
    public static void main(String[] args) {
        // Look up: Using ternary operator for clean default handling
        String name = (args.length > 0) ? args[0] : "World";
        
        System.out.println("Hello, " + name + "!");
    }
}
